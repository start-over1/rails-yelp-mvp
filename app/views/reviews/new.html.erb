<div class="container">
  <h1 class="text-center mt-5">Add Review</h1>

  <%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :rating %>
    <%= f.input :content %>
    <%= f.submit class: "btn btn-primary"%>
  <% end %>

</div>
